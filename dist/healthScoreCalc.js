"use strict";
// function calculateHealthScore(obdData) {
//     let score = 0;
Object.defineProperty(exports, "__esModule", { value: true });
//     // Fuel System Status
//     if (obdData.fuelSystemStatus === 'Closed Loop') {
//       score += 10;
//     } else {
//       score -= 5;
//     }
//     // Engine Load
//     if (obdData.engineLoad >= 20 && obdData.engineLoad <= 80) {
//       score += 10;
//     } else if (obdData.engineLoad > 80) {
//       score -= 5;
//     }
//     // RPM
//     if (obdData.rpm >= 1000 && obdData.rpm <= 3000) {
//       score += 10;
//     } else if (obdData.rpm > 4000) {
//       score -= 5;
//     }
//     // Speed
//     if (obdData.vehicleSpeed <= 80) {
//       score += 5;
//     } else {
//       score -= 5;
//     }
//     // Fuel Trim
//     if (obdData.fuelTrim >= -10 && obdData.fuelTrim <= 10) {
//       score += 10;
//     } else {
//       score -= 5;
//     }
//     // Intake Manifold Pressure
//     if (obdData.intakeManifoldPressure >= 9 && obdData.intakeManifoldPressure <= 12) {
//       score += 10;
//     } else {
//       score -= 5;
//     }
//     const maxScore = 55;  // Max possible score
//     return (score / maxScore) * 100;  // Normalize score to a scale of 0-100
//   }
//   module.exports = calculateHealthScore;
//# sourceMappingURL=healthScoreCalc.js.map